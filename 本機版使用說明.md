# 泰聚達本機版

這一版會在你的 Windows 電腦上運行，不會公開到網際網路。

## 啟動

雙擊 `啟動泰聚達本機版.cmd`。啟動完成後會自動開啟：

- 前台：http://127.0.0.1:3000/
- 文章管理後台：http://127.0.0.1:3000/admin/

第一次啟動可能需要較長時間，之後通常會更快。

## 停止與查看狀態

- 要停止網站，雙擊 `停止泰聚達本機版.cmd`。
- 要確認網站是否仍在運行，雙擊 `查看泰聚達本機版狀態.cmd`。

## 資料保存

文章、草稿及修訂紀錄會保存在專案內的 `.local-data` 資料夾。停止網站或重新啟動電腦不會刪除資料。

不要手動刪除 `.local-data` 資料夾；刪除後本機文章資料也會一併消失。購物車則保存在 `http://127.0.0.1:3000` 使用的瀏覽器內。

請固定使用 `127.0.0.1`，不要改成 `localhost`；這台電腦另有其他本機網站使用 `localhost:3000`。

如需備份文章資料，請先停止網站，再複製整個 `.local-data` 資料夾到其他安全位置。

## 使用範圍

這是單機版，預設只有這台電腦能開啟。它不包含線上金流，也不會取代目前已公開的 GitHub Pages 前台。
