# 泰聚達本機版

這一版會在你的 Windows 電腦上運行，不會公開到網際網路。

## 啟動

雙擊 `啟動泰聚達本機版.cmd`。啟動完成後會自動開啟：

- 前台：http://127.0.0.1:3000/
- 營運總覽：http://127.0.0.1:3000/admin/
- 文章管理後台：http://127.0.0.1:3000/admin/articles/
- 商品與庫存：http://127.0.0.1:3000/admin/products/
- 訂單管理：http://127.0.0.1:3000/admin/orders/
- 網站編輯器：http://127.0.0.1:3000/admin/site/
- 本機會員測試：http://127.0.0.1:3000/account/

第一次啟動可能需要較長時間，之後通常會更快。

## 停止與查看狀態

- 要停止網站，雙擊 `停止泰聚達本機版.cmd`。
- 要確認網站是否仍在運行，雙擊 `查看泰聚達本機版狀態.cmd`。

## 資料保存

文章、草稿、修訂紀錄、商品、庫存、訂單及庫存流水會保存在專案內的 `.local-data` 資料夾。停止網站或重新啟動電腦不會刪除資料。

不要手動刪除 `.local-data` 資料夾；刪除後本機營運資料也會一併消失。購物車、會員測試資料與訂單摘要則保存在 `http://127.0.0.1:3000` 使用的瀏覽器內。

請固定使用 `127.0.0.1`，不要改成 `localhost`；這台電腦另有其他本機網站使用 `localhost:3000`。

如需備份營運資料，直接雙擊 `備份泰聚達本機資料.cmd`。啟動器會先安全暫停網站，把完整資料複製到 `.local-backups`，再自動重新啟動。至少每週把最新備份再複製到另一顆硬碟或可信任的雲端空間；同一顆硬碟上的備份無法防止硬碟損壞。

### 驗證備份

雙擊 `驗證泰聚達本機備份.cmd`，再拖曳或貼上要檢查的「單一、確切備份資料夾」。驗證全程只讀，不會修改備份或 `.local-data`，而且不會自動猜測哪一份是最新備份。

目前版本建立的是 v2 備份。清單會逐檔記錄相對路徑、大小與 SHA-256；驗證時也會拒絕多出的檔案、遺失檔案、雜湊或大小不符、符號連結、接合點與路徑穿越。命令列用法：

    npm run local:backup:verify -- "C:\確切路徑\taijuda-data-YYYYMMDD-HHMMSS"

舊版 v1 備份仍可檢查基本格式與檔案安全，但沒有逐檔雜湊，所以不能直接還原。請先用舊資料啟動可運行的網站，再雙擊 `備份泰聚達本機資料.cmd`，重新建立 v2 備份。

### 還原資料

1. 先雙擊 `停止泰聚達本機版.cmd`，確定網站已停止。
2. 雙擊 `還原泰聚達本機資料.cmd`。
3. 拖曳或貼上要使用的單一 v2 備份資料夾，仔細確認後按 `Y`。
4. 還原程式會再次完整驗證備份，並確認本機服務及連接埠 3000 都已停止。
5. 程式只會寫入此專案的 `.local-data`。原有資料會先另存為 `.local-backups/taijuda-pre-restore-*`，再透過同磁碟暫存資料夾切換；若中途失敗會嘗試自動回復。
6. 成功訊息只代表檔案集合、大小與雜湊一致，不代表網站已健康。最後請雙擊 `啟動泰聚達本機版.cmd`，等待資料庫 migration 與 health 驗證完成，再進入後台確認文章、商品、庫存與訂單。

命令列用法：

    npm run local:stop
    npm run local:restore -- "C:\確切路徑\taijuda-data-YYYYMMDD-HHMMSS"
    npm run local:start

不要把專案根目錄、使用者家目錄、磁碟根目錄、`.local-backups` 根目錄或含有 `*`、`?`、`..` 的路徑交給還原程式。還原失敗時請保留畫面與 `.local-backups/taijuda-pre-restore-*`，不要反覆重試或手動刪除暫存資料夾。

## 編輯文章

文章後台提供標題、摘要、正文與右側設定欄。工具列支援 H2–H4、粗體、斜體、底線、刪除線、行內程式碼、清單、引用、程式碼區塊、分隔線、連結與復原；不提供 H1，避免公開文章出現重複主標題。

右側可切換「發布、SEO、圖片、版本」。目前圖片採公開網址，尚未接雲端檔案上傳；版本還原會建立新的草稿，不會刪除既有紀錄。

## 本機會員測試

`/account/` 的 Email 驗證碼只是在本機模擬登入流程，不會真的寄信，也尚未串接 LINE Login。請使用測試用電子郵件，不要輸入真實個資。

目前個人資料、購物車與訂單摘要仍以瀏覽器為單位保存，尚未依登入帳號隔離，也不會跨裝置同步。切換不同測試 Email 不代表資料已分成不同會員；會員、身分、Session、地址與訂單 `member_id` 等資料結構雖已預建，正式上線前仍須串接伺服器 Session／登入 API、帳號資料隔離與查詢授權。

## 把後台內容同步到公開版

文章或頁面按下「發布」後，本機資料已儲存，但 GitHub 公開站還不會自動改變。請雙擊 `同步並建立泰聚達公開版.cmd`：

1. 啟動並確認本機後台可用。
2. 只匯出已發布的頁面、文章與可公開商品，不會匯出訂單、客戶資料或管理員身分。
3. 驗證 SEO 必填欄位並重建 `out` 靜態網站。
4. 完成後再把 Git 變更推送到 `main`，GitHub Pages 才會更新。

商品未填妥實物圖片、圖片替代文字及足夠的 SEO 資料前，不可啟用「搜尋收錄狀態」；未確認商品會維持 `noindex` 並排除在 sitemap 之外。

## 使用範圍

這是單機版，預設只有這台電腦能開啟。它不包含線上金流，也不會取代目前已公開的 GitHub Pages 前台。圖片目前使用網址，尚未提供檔案上傳媒體庫。若要讓陌生訪客直接送出訂單，正式 Worker 環境仍須設定管理員名單並加上 Turnstile 或 Rate Limiting。
